module ea05 where

open import lib
open import secret

-- ez az eloadas 10 perccel rovidebb

-- Vec A : ℕ → Set, List : Set → Set, polymorphism is special case

-- zeroes-l, zeros-v, test it
-- append-l, append-v with explicit params, test it
-- append-v with implicit params, make it infix

-- _!!_ for List, Maybe-s, Fin, Fin-es verzio vektorokra

-- Π types, szabalyok, special case simply typed

-- Σ types, szabalyok, FlexVec

{-
ez jovo oran lesz:

    Π     Σ
   / \   / \
  /   \ /   \
 →     ×     ⊎
-}
