module ea09 where

open import lib

-- vizsgak: majdnem minden szeran 10-12 kozott, gepteremben, peldavizsga

-- Ez az előadás 13 perccel rövidebb.

-- kommutativ monoid

-- egyenlet : (m n : ℕ) → (m + m) + (n + 0) ≡ m + (n + m)

-- nemnem12 : ¬ ((n : ℕ) → ¬ (n ≡ 1) → n ≡ 2)

-- klassz vs konstr

